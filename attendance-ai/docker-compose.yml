version: '3.8'

services:
  api:
    build: .
    ports:
      - "8000:8000"
    environment:
      - DATABASE_URL=${DATABASE_URL}
    volumes:
      - ./model:/app/model
      - ./tokenizer:/app/tokenizer
    restart: always
